management:
  endpoint:
    shutdown:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
    jmx:
      exposure:
        include: "*"

logging:
  level:
    root: ERROR
    org.springframework: ERROR
    com.perso.cluedohelper: DEBUG
    ExternalRequest: ERROR
  config: classpath:log4j/log4j2-local.yml
